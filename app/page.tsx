'use client'
import Link from 'next/link'
import { useEffect, useState } from 'react'
import { Sparkles } from 'lucide-react'
import { continents } from '@/lib/world'
import { defaultSave, readSave, type Save } from '@/lib/progress'
export default function Home() { const [save, setSave] = useState<Save>(defaultSave); useEffect(() => setSave(readSave()), []); return <main className="min-h-screen bg-background px-5 py-6 text-foreground"><div className="mx-auto flex max-w-xl flex-col gap-8"><header className="flex items-center justify-between"><div><div className="flex items-center gap-2 text-sm font-bold text-primary"><Sparkles className="size-4" /> さわって わかる</div><h1 className="mt-2 text-3xl font-bold">さんすう・りか</h1></div><Link href="/bag" className="rounded-2xl bg-card px-4 py-3 text-xl shadow-sm" aria-label="もちもの">🎒</Link></header><div className="flex items-center justify-between rounded-2xl bg-card px-5 py-4 shadow-sm"><span className="font-bold">✨ {save.points} キラ</span><span className="text-sm text-muted-foreground">すきな たいりくへ</span></div><section className="flex flex-col gap-4">{continents.map((c) => <Link key={c.id} href={`/c/${c.id}`} className="flex items-center gap-5 rounded-[2rem] border border-border bg-card p-6 shadow-sm transition hover:-translate-y-1"><span className="text-6xl">{c.emoji}</span><span className="flex-1"><span className="block text-xl font-bold">{c.name}</span><span className="mt-2 block h-3 overflow-hidden rounded-full bg-muted"><span className="block h-full rounded-full" style={{ width: c.id === 'science' ? '18%' : '8%', backgroundColor: c.accent }} /></span><span className="mt-2 block text-xs text-muted-foreground">すすみぐあいを みる</span></span><span className="text-2xl text-muted-foreground">›</span></Link>)}</section></div></main> }
