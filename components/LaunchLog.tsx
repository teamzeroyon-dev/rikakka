'use client'
export type LaunchRecord = { stretch: number; distance: number; hit?: boolean; predicted?: boolean }
export function LaunchLog({ records, courseLength }: { records: LaunchRecord[]; courseLength: number }) {
 return <section className="rounded-2xl border border-border bg-card p-3 shadow-sm" aria-label="発射のきろく"><h2 className="mb-2 font-bold">きろく</h2><div className="grid grid-cols-[2rem_5rem_1fr_5rem_1.5rem] gap-2 border-b border-border pb-2 text-xs font-bold text-muted-foreground"><span>かい</span><span>のばした長さ</span><span>すすんだきょり</span><span /><span /></div><div className="flex max-h-52 flex-col overflow-y-auto">{records.length === 0 ? <p className="py-4 text-center text-sm text-muted-foreground">はなすと きろくが のこるよ</p> : [...records].reverse().map((r, i) => <div key={`${r.stretch}-${i}`} className="grid grid-cols-[2rem_5rem_1fr_5rem_1.5rem] items-center gap-2 border-b border-border/60 py-2 text-sm"><span>{records.length - i}</span><span>{r.stretch}cm</span><span>{Math.round(r.distance)}cm</span><span className="h-2 rounded-full bg-primary" style={{ width: `${Math.max(4, Math.min(100, r.distance / courseLength * 100))}%` }} aria-label={`${Math.round(r.distance)}cmの棒グラフ`} /><span aria-label={r.hit ? 'ゴールに とどいた' : 'まだ とどいていない'}>{r.hit ? '🎉' : '➖'}</span></div>)}</div></section>
}
